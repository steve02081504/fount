# Default cabinet template

Files and folders placed here are imported into each user's `cabinet_id: default` cabinet on first open (`overwrite: false` — existing entries are kept).

This directory is a future hook for built-in tools and starter content.
