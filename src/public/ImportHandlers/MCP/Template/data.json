{
  "name": "mcp_example",
  "description": "Example MCP server plugin",
  "description_markdown": "Example MCP (Model Context Protocol) server plugin",
  "tags": ["example"],
  "config": {
    "command": "npx",
    "args": [
      "-y",
      "@modelcontextprotocol/server-filesystem",
      "/path/to/directory"
    ],
    "env": {
      "NODE_ENV": "production"
    }
  }
}

